function scrollUp() {
  window.scrollBy({
    top: -500,
    left: 0,
    behavior: 'smooth'
  });
}

function scrollDown() {
  window.scrollBy({
    top: 500,
    left: 0,
    behavior: 'smooth'
  });
}
